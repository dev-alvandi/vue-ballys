import { describe, it, expect } from 'vitest'

import { mount } from '@vue/test-utils'
import PlayersList from '../Players/PlayersList.component.vue'

describe('PlayersList component', () => {
  it('renders properly', () => {})
})
